<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Hyre" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/button_green</item>
        <item name="android:statusBarColor">@color/background_green</item>
    </style>

    <style name="Theme.Hyre" parent="Base.Theme.Hyre" />
</resources>